[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nekrozvpn"
version = "0.1.0"
description = "Builder for Nekroz VPN"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Nekroz" }
]

[tool.setuptools]
py-modules = ["main"]
